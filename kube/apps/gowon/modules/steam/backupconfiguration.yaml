---
apiVersion: stash.appscode.com/v1beta1
kind: BackupConfiguration
metadata:
  name: gowon-steam
  namespace: gowon
spec:
  repository:
    name: gowon-steam
  schedule: "40 16 * * *"
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: steam
    volumeMounts:
      - name: data
        mountPath: /data
    paths:
      - /data
    exclude:
      - /data/lost+found
  retentionPolicy:
    name: 'keep-last-week'
    keepLast: 7
    prune: true
  timeOut: 5m
