---
apiVersion: stash.appscode.com/v1beta1
kind: BackupConfiguration
metadata:
  name: gowon-lastfm
  namespace: gowon
spec:
  repository:
    name: gowon-lastfm
  schedule: "10 16 * * *"
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: lastfm
    volumeMounts:
      - name: data
        mountPath: /data
    paths:
      - /data
    exclude:
      - /data/lost+found
  retentionPolicy:
    name: 'keep-last-week'
    keepLast: 7
    prune: true
  timeOut: 5m
