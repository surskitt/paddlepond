---
apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: lastfm
  namespace: gowon
spec:
  repository:
    name: lastfm
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: lastfm
    volumeMounts:
      - name: data
        mountPath: /data
    rules:
      - paths:
          - /data
