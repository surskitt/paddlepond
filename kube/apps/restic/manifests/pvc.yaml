---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: restic-data
  namespace: restic
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
