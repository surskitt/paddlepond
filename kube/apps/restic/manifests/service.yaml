---
apiVersion: v1
kind: Service
metadata:
  name: restic
  namespace: restic
  labels:
    app: restic
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: restic
