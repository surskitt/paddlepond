---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rtsp-simple-server
  namespace: home-assistant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rtsp-simple-server
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: rtsp-simple-server
    spec:
      containers:
        - name: rtsp-simple-server
          image: aler9/rtsp-simple-server:v0.17.8
          env:
            - name: RTSP_PROTOCOLS
              value: tcp
      restartPolicy: Always
