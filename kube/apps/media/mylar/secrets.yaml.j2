---
apiVersion: v1
kind: Secret
metadata:
  name: mylar-secrets
  namespace: media
type: Opaque
stringData:
  values: |
    ingress:
      main:
        hosts:
          - host: {{ pass_mylar_host }}
            paths:
              - path: /
                pathType: Prefix
        annotations:
          external-dns.alpha.kubernetes.io/target: {{ pass_cname_host }}
        tls:
          - hosts:
              - {{ pass_mylar_host }}
            secretName: mylar-tls
