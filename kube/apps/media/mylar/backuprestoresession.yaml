---
apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: mylar
  namespace: media
spec:
  repository:
    name: mylar
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: mylar
    volumeMounts:
      - name: config
        mountPath: /config
    rules:
      - paths:
          - /config
