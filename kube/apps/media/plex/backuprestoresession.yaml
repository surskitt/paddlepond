---
apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: plex
  namespace: media
spec:
  repository:
    name: plex
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: plex
    volumeMounts:
      - name: config
        mountPath: /config
    rules:
      - paths:
          - /config
