---
apiVersion: stash.appscode.com/v1beta1
kind: BackupConfiguration
metadata:
  name: nzbget
  namespace: media
spec:
  repository:
    name: nzbget
  schedule: "0 15 * * *"
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: nzbget
    volumeMounts:
      - name: config
        mountPath: /config
    paths:
      - /config
  runtimeSettings:
    container:
      securityContext:
        runAsUser: 568
        runAsGroup: 568
  retentionPolicy:
    name: 'keep-last-week'
    keepLast: 7
    prune: true
  timeOut: 5m
