---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: forked-daapd-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
  storageClassName: nfs-client
