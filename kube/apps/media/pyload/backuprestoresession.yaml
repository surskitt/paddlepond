---
apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: pyload
  namespace: media
spec:
  repository:
    name: pyload
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: pyload
    volumeMounts:
      - name: config
        mountPath: /config
    rules:
      - paths:
          - /config
