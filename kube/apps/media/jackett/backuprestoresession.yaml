---
apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: jackett
  namespace: media
spec:
  repository:
    name: jackett
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: jackett
    volumeMounts:
      - name: config
        mountPath: /config
    rules:
      - paths:
          - /config
