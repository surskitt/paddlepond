---
apiVersion: v1
kind: Secret
metadata:
  name: nzbhydra2-secrets
  namespace: media
type: Opaque
stringData:
  values.yml: |
    ingress:
      hosts:
        - host: ${NZBHYDRA2_HOST}
          paths:
            - path: /
              pathPrefix: Prefix
      annotations:
        external-dns.alpha.kubernetes.io/target: ${CNAME_HOST}
      tls:
        - hosts:
            - ${NZBHYDRA2_HOST}
          secretName: nzbhydra2-tls
