---
apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: calibre-web
  namespace: media
spec:
  repository:
    name: calibre-web
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: calibre-web
    volumeMounts:
      - name: config
        mountPath: /config
    rules:
      - paths:
          - /config
