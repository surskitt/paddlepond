---
apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: barcodebuddy
  namespace: grocy
spec:
  repository:
    name: barcodebuddy
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: barcodebuddy
    volumeMounts:
      - name: config
        mountPath: /config
    rules:
      - paths:
          - /config
