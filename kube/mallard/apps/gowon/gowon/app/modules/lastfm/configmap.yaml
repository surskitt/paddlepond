---
apiVersion: v1
kind: ConfigMap
metadata:
  name: lastfm-config
  namespace: gowon
  labels:
    gowon/enabled: "true"
data:
  config.yaml: |
    commands:
      - command: lastfm
        endpoint: http://lastfm:8080
