---
apiVersion: v1
kind: Secret
metadata:
  name: private
  namespace: flux-system
type: Opaque
stringData:
  identity: |
    {{ pass_flux_private_identity | indent(4) }}
  identity.pub: |
    {{ pass_flux_private_identity_pub | indent(4) }}
  known_hosts: |
    {{ pass_flux_known_hosts | indent(4) }}
