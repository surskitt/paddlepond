---
apiVersion: v1
kind: ConfigMap
metadata:
  name: speedtest-tracker-gatus-ep
  namespace: monitoring
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: speedtest-tracker
        group: monitoring
        url: https://speedtest.${SECRET_DOMAIN}
        interval: 1m
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: custom
          - type: telegram
