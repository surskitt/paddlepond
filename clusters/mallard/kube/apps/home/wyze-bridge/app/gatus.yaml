---
apiVersion: v1
kind: ConfigMap
metadata:
  name: wyze-bridge-gatus-ep
  namespace: home
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: wyze-bridge
        group: home
        url: https://wyze.${SECRET_DOMAIN}
        interval: 1m
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: custom
          - type: telegram
