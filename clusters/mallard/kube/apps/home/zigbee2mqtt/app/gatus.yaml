---
apiVersion: v1
kind: ConfigMap
metadata:
  name: zigbee2mqtt-gatus-ep
  namespace: home
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: zigbee2mqtt
        group: home
        url: https://zigbee2mqtt.${SECRET_DOMAIN}
        interval: 1m
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: custom
          - type: telegram
