---
apiVersion: v1
kind: ConfigMap
metadata:
  name: lastfm-config
  namespace: gowon
  labels:
    gowon/enabled: "true"
data:
  config.yaml: |
    commands:
      - command: lastfm
        endpoint: http://mqttbridge:8080
        help: show last listened tracks on last.fm
